#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// The Multiplicative Order
// smallest k that achieves: a^k ≡ 1 (mod n) when a and n are relatively prime (their gcd(a, n) = 1)

// Euler's totient function φ(n)
// counts the numbers(x) up to a given integer n that are relatively prime to it.
// Two numbers are relatively prime if they don't share common prime factors (their gcd(x, n) = 1)

// find the least count(c) in 10^c ≡ 1 (mod 9n/d), c (The Multiplicative Order) exists where gcd(10, 9n/d) = 1

